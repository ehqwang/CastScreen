<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <View
        android:id="@+id/bg_top_view"
        android:layout_width="match_parent"
        android:layout_height="45dp"
        app:layout_constraintTop_toTopOf="parent"
        android:background="#000000"
        />

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@+id/bg_top_view"
        app:layout_constraintBottom_toBottomOf="@+id/bg_top_view"
        app:layout_constraintStart_toStartOf="@+id/bg_top_view"
        app:layout_constraintEnd_toEndOf="@+id/bg_top_view"
        android:textColor="@color/white"
        android:text="投放"
        />

    <TextView
        android:id="@+id/tv_speed"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@+id/bg_top_view"
        app:layout_constraintBottom_toBottomOf="@+id/bg_top_view"
        app:layout_constraintEnd_toEndOf="@+id/bg_top_view"
        android:layout_marginEnd="10dp"
        />

    <SurfaceView
        android:id="@+id/surface"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@+id/bg_top_view"
        app:layout_constraintBottom_toBottomOf="parent"
        />

    <TextView
        android:id="@+id/tv_hint"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="@+id/surface"
        app:layout_constraintBottom_toBottomOf="@+id/surface"
        android:background="#723472"
        android:text="未开始"
        android:gravity="center"
        android:textColor="@color/white"
        />


</androidx.constraintlayout.widget.ConstraintLayout>